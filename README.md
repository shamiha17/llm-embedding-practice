# LLM & Embedding Practice

Today's learning:

- LLM API integration using Groq
- Prompt and message roles
- Text embeddings using Sentence Transformers
- Vector representation
- Cosine similarity
- Semantic similarity

## Example

"The cow has fever"
vs
"The cow is sick"

Cosine similarity: 0.8358

This shows that semantically similar sentences can have high similarity scores.
